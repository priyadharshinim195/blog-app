services:
  - type: web
    name: blog-app
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py migrate && python manage.py loaddata blogdata.json
    startCommand: gunicorn mysite.wsgi:application